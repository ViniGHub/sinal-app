node_modules/
dist/
.vite/
*.local
.DS_Store

# local env overrides (may hold TURN credentials)
.env
.env.local
.env.*.local

# Where `wrangler dev` reads secrets from, in the clear. Never commit it —
# the real values live in Cloudflare, set with `wrangler secret put`.
.dev.vars
.dev.vars.*
.wrangler/

# Playwright
/test-results/
/playwright-report/
/blob-report/
